name: CI
# P5 long-run exact-head trigger

on:
  push:
    branches:
      - main
      - feature/self-healing-longrun
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lifetime:
    name: P5 Lifetime Gate
    runs-on: macos-15
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install P5 test runtime dependencies
        run: |
          command -v rg >/dev/null 2>&1 || brew install ripgrep

      - name: Typecheck lifetime control plane
        run: npm run typecheck

      - name: Soak P5 long-run state machine
        shell: bash
        run: |
          set -euo pipefail
          for pass in 1 2 3 4; do
            echo "P5 long-run soak pass $pass/4"
            npx vitest run test/long-run.test.ts test/long-run-runtime.test.ts
          done

      - name: Exercise P5 cross-module fault matrix
        run: >
          npx vitest run
          test/self-healing.test.ts
          test/session-input.test.ts
          test/agents.test.ts
          test/bridge.test.ts
          test/mcp.test.ts

  verify:
    needs: lifetime
    name: Verify macOS arm64
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify
        run: npm run verify:ci

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'

      - name: Install plugin Python runtime
        run: python -m pip install uv==0.12.5

      - name: Exercise published plugins on this OS
        env:
          COS_PLUGIN_LIVE_TEST: '1'
        run: npx vitest run test/plugins-live-proxy.test.ts test/plugins-python-live.test.ts test/plugins-catalog-live.test.ts
